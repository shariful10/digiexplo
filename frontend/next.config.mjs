/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
         "dummyimage.com",
         "images.remotePatterns",
         "mayosis.themepreview.xyz",
      ],
	},
};

export default nextConfig;
