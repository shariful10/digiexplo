import React from "react";

const MobileNav = () => {
	return (
		<div className="md:hidden">
			<h1 className="text-2xl font-bold text-primary">MobileNav only for mobile view</h1>
		</div>
	);
};

export default MobileNav;
